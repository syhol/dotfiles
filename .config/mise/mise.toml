# Canonical mise config for syhol's dotfiles.
#
# This file is the single source of truth. The [dotfiles] section below
# symlinks the whole .config/mise/ dir to ~/.config/mise/ so this config is
# loaded globally (mise reads ~/.config/mise/mise.toml) and `mise activate`
# (wired into every shell rc) picks it up.
#
# First-time setup on a new machine:
#   git clone https://github.com/syhol/dotfiles ~/Code/syhol/dotfiles
#   cd ~/Code/syhol/dotfiles
#   curl https://mise.run | sh
#   mise bootstrap

[tools]
node = "latest"
deno = "latest"
bun = "latest"
aube = "latest"
uv = "latest"
go = "latest"
usage = "4"
claude = "latest"
codex = "latest"
biome = "latest"
dprint = "latest"
"github:earendil-works/pi" = "latest"
"github:Dashlane/dashlane-cli" = "latest"
"github:janosmiko/lfk" = "latest"
"github:schpet/linear-cli" = "1.10.0"
"github:vercel-labs/agent-browser" = "latest"
# "http:acme.sh" = { version = "1.0.0", url = "https://raw.githubusercontent.com/acmesh-official/acme.sh/master/acme.sh" }
"npm:@nubjs/nub" = "latest"
"pipx:twg" = "latest"
ruby = "3"

[settings]
experimental = true
color_theme = "catppuccin"
idiomatic_version_file_enable_tools = []
dotfiles.root = "~/Code/syhol/dotfiles"
# symlink-each (not whole-dir symlink) keeps each config dir a REAL directory
# with only tracked files symlinked. Apps write history/state/secrets
# (e.g. zsh .zsh_history/secrets.zsh, shell/secrets.sh, helix/runtime) into
# those dirs at runtime — symlink-each keeps those local instead of routing
# them back into this git repo.
dotfiles.default_mode = "symlink-each"

[settings.pipx]
uvx = true

[settings.npm]
package_manager = "aube"

# --- Dotfiles -----------------------------------------------------------------
# The repo root mirrors $HOME. With dotfiles.root set, an entry's source is
# inferred from its home-relative target, so most entries are empty {}. Default
# mode is symlink-each; overrides are noted inline.
[dotfiles]
# Single-file entries (mode = symlink; symlink-each only works on directories).
# .bashrc / .zshenv / VS Code settings keep tidy source paths under .config via
# an explicit source, while the symlink still lands at the real target.
"~/.bashrc" = { source = "~/Code/syhol/dotfiles/.config/bash/bashrc", mode = "symlink" }
"~/.zshenv" = { source = "~/Code/syhol/dotfiles/.config/zsh/zshenv", mode = "symlink" }
"~/.hushlogin" = { mode = "symlink" }
"~/.ignore" = { mode = "symlink" }
"~/.ssh/config" = { mode = "symlink" }
"~/Library/Application Support/Code/User/settings.json" = { source = "~/Code/syhol/dotfiles/.config/vscode/settings.json", mode = "symlink" }
# mise's own config dir: symlink-each links mise.toml, mise.lock, and tasks/*
# into ~/.config/mise/ so the config loads globally and `mise run sync` etc.
# resolve the file tasks from anywhere.
"~/.config/mise" = {}

# Personal scripts on PATH (mx, themeset, vid-smol). symlink-each so the
# runtime mise binary and other installed executables in ~/.local/bin coexist.
"~/.local/bin" = {}

# Config dirs — symlink-each (the default): tracked files become symlinks while
# the directory itself stays real, so runtime files (yazi plugins via
# `ya pkg install`, zsh history/compdump, helix runtime, btop state, etc.)
# coexist locally without polluting the repo.
"~/.config/bash" = {}
"~/.config/bat" = {}
"~/.config/broot" = {}
"~/.config/btop" = {}
"~/.config/delta" = {}
"~/.config/dprint" = {}
"~/.config/fish" = {}
"~/.config/gh-dash" = {}
"~/.config/ghostty" = {}
"~/.config/git" = {}
"~/.config/helix" = {}
"~/.config/kitty" = {}
"~/.config/nvim" = {}
"~/.config/shell" = {}
"~/.config/themes" = {}
"~/.config/wezterm" = {}
"~/.config/yazi" = {}
"~/.config/zsh" = {}
"~/.config/starship.toml" = { mode = "symlink" }
"~/.config/topgrade.toml" = { mode = "symlink" }

# gh and k9s rewrite their files at runtime (auth metadata / UI state). Copy
# instead of symlink so those writes don't churn the git repo.
"~/.config/gh" = { mode = "copy" }
"~/.config/k9s" = { mode = "copy" }

# --- Bootstrap: login shell ---------------------------------------------------
[bootstrap.user]
login_shell = "/opt/homebrew/bin/fish"

[bootstrap.repos]
"~/Code/syhol/knowledge" = { url = "git@github.com:syhol/knowledge.git" }
"~/Code/syhol/dotfiles" = { url = "git@github.com:syhol/dotfiles.git" }

# --- Bootstrap: package-manager plugins ---------------------------------------
# Installed in bootstrap's plugin phase, before the packages they manage.
# `vscode:` entries in [bootstrap.packages] are VS Code extensions, driven by
# the `code` CLI (replaces the old extensions.txt + system:vscode-extensions task).
[bootstrap.plugins]
vscode = "https://github.com/syhol/mise-plugin-vscode.git"

# --- Bootstrap: system packages (Homebrew formulae + casks) -------------------
# `mise bootstrap packages install` installs these straight into the Homebrew
# prefix: `brew:` formulae and `brew-cask:` casks (mise's brew-cask backend now
# handles `app`, `pkg`, `binary`, `font`, `manpage`, `command_wrapper` and
# `*_completion` artifacts, so the old Brewfile + `brew bundle` step is gone).
# Casks still land in the Caskroom and show up in `brew list --cask`.
# VS Code extensions are `vscode:` entries at the bottom of this section.
# NOTE: removing an entry here does NOT uninstall the package by itself — run
# `mise bootstrap packages prune` (or `brew uninstall`) to drop it.
[bootstrap.packages]
# Kubernetes & Docker
"brew:kubernetes-cli" = "latest"
"brew:kubectx" = "latest"
"brew:helm" = "latest"
"brew:lazydocker" = "latest"
"brew:k9s" = "latest"

# Shell
"brew:bash" = "latest"              # Modern bash (macOS ships 3.2)
"brew:bash-completion@2" = "latest" # Bash tab completions
"brew:fish" = "latest"              # Alternative shell
"brew:vivid" = "latest"             # ls colors theme generator
"brew:starship" = "latest"          # shell prompt
"brew:herdr" = "latest"

# Shell utilities
"brew:coreutils" = "latest"
"brew:gawk" = "latest"        # Required by fzf-tab-completion
"brew:gnu-sed" = "latest"     # Required by fzf-tab-completion
"brew:grep" = "latest"        # Required by fzf-tab-completion (installs as ggrep)
"brew:zoxide" = "latest"      # cd alternative - change directories
"brew:eza" = "latest"         # ls alternative - list directory contents
"brew:bat" = "latest"         # cat alternative - print file contents
"brew:difftastic" = "latest"  # diff alternative - print the diff between files
"brew:git-delta" = "latest"   # diff alternative - print the diff between files
"brew:hyperfine" = "latest"   # time alternative - benchmark cli commands
"brew:procs" = "latest"       # ps alternative - list os processes
"brew:fd" = "latest"          # find alternative - search files by file name
"brew:sd" = "latest"          # sed alternative - replace text
"brew:choose-rust" = "latest" # cut alternative - extract text
"brew:ugrep" = "latest"       # grep alternative - search files by contents (same API as gnu/bsd grep)
"brew:ripgrep" = "latest"     # grep alternative - search files by contents
"brew:dust" = "latest"        # du alternative - show directory disk usage
"brew:btop" = "latest"        # top alternative - show processes using the most resources
"brew:viddy" = "latest"       # watch alternative - rerun a command every n seconds
"brew:sevenzip" = "latest"    # tar/zip alternative - compress and decompress archives
"brew:ouch" = "latest"        # tar/zip alternative - compress and decompress archives
"brew:tealdeer" = "latest"    # man alternative - help pages
"brew:glow" = "latest"        # markdown viewer
"brew:yazi" = "latest"        # filesystem explorer
"brew:broot" = "latest"       # filesystem explorer
"brew:fzf" = "latest"         # fuzzy finder
"brew:topgrade" = "latest"    # multi-system updater

# Data processing
"brew:jq" = "latest"
"brew:yq" = "latest"
"brew:jless" = "latest"
"brew:otree" = "latest"
"brew:trurl" = "latest"
"brew:xsv" = "latest"
"brew:pgcli" = "latest"
"brew:lazysql" = "latest"

# Crpyto
"brew:age" = "latest"
"brew:minisign" = "latest"

# Media
"brew:ffmpeg" = "latest"
"brew:imagemagick" = "latest"

# Git & VCS
"brew:gh" = "latest"         # github cli
"brew:git-extras" = "latest" # extra git commands
"brew:git-town" = "latest"   # stacked pr manager
"brew:lazygit" = "latest"    # tui for git (go)

# Neovim & Editors & Lua based tooling
"brew:neovim" = "latest"
"brew:luarocks" = "latest"
"brew:tree-sitter-cli" = "latest"
"brew:stylua" = "latest"

# Http & Net
"brew:curl" = "latest"
"brew:wget" = "latest"
"brew:somo" = "latest"  # port scanner
"brew:gping" = "latest" # ping alternative - test ip address reachability and latency
"brew:oha" = "latest"   # ab alternative - benchmark http endpoints
"brew:xh" = "latest"    # curl alternative - make http requests
"brew:doggo" = "latest" # dig alternative - lookup dns records (since dog is dead)

# Capably
"brew:awscli" = "latest"
"brew:watch" = "latest"
"brew:cairo" = "latest"
"brew:libolm" = "latest"
"brew:automake" = "latest"
"brew:libxmlsec1" = "latest"
"brew:pkgconf" = "latest"
"brew:poppler" = "latest"
"brew:postgresql@18" = "latest"
"brew:postgresql@14" = "latest"
"brew:swig" = "latest"
"brew:llvm" = "latest"
"brew:dbus" = "latest"
"brew:openssl@3" = "latest"
"brew:pango" = "latest"
"brew-cask:silverstein/tap/minutes" = "latest"

# --- Casks --------------------------------------------------------------------
# Capably / productivity
"brew-cask:libreoffice" = "latest"
"brew-cask:1password" = "latest"
"brew-cask:codex" = "latest"
"brew-cask:tailscale-app" = "latest"

# Fonts
"brew-cask:font-meslo-lg-nerd-font" = "latest"
"brew-cask:font-jetbrains-mono-nerd-font" = "latest"

# Apps
"brew-cask:google-chrome" = "latest"
"brew-cask:docker-desktop" = "latest"
"brew-cask:ghostty" = "latest"
"brew-cask:kitty" = "latest"
"brew-cask:wezterm" = "latest"
"brew-cask:visual-studio-code" = "latest"
"brew-cask:monitorcontrol" = "latest"
"brew-cask:rectangle" = "latest"
"brew-cask:mullvad-vpn" = "latest"
"brew-cask:slack" = "latest"
"brew-cask:keepassxc" = "latest"
"brew-cask:syncthing-app" = "latest"
"brew-cask:steam" = "latest"
"brew-cask:discord" = "latest"
"brew-cask:handy" = "latest"

# --- VS Code extensions (vscode plugin) ---------------------------------------
# VS Code Languages
"vscode:denoland.vscode-deno" = "latest" # js/ts - deno
"vscode:oven.bun-vscode" = "latest" # js/ts - bun
"vscode:tamasfe.even-better-toml" = "latest" # toml
"vscode:redhat.vscode-yaml" = "latest" # yaml
"vscode:yzhang.markdown-all-in-one" = "latest" # markdown
"vscode:hashicorp.terraform" = "latest" # hcl
"vscode:matheusq94.tfs" = "latest" # hcl
"vscode:mechatroner.rainbow-csv" = "latest" # csv
"vscode:ms-python.debugpy" = "latest" # python
"vscode:ms-python.python" = "latest" # python
"vscode:ms-python.vscode-pylance" = "latest" # python
"vscode:ms-python.vscode-python-envs" = "latest"
"vscode:golang.go" = "latest" # go
"vscode:rust-lang.rust-analyzer" = "latest" # rust
"vscode:ms-vscode.makefile-tools" = "latest" # c/make
"vscode:jakebecker.elixir-ls" = "latest" # elixir
"vscode:thenuprojectcontributors.vscode-nushell-lang" = "latest" # nu
"vscode:ms-azuretools.vscode-docker" = "latest" # docker (microsoft)
"vscode:ms-azuretools.vscode-containers" = "latest" # docker (microsoft)
"vscode:docker.docker" = "latest" # docker (docker inc)

# VS Code Language Extras
"vscode:biomejs.biome" = "latest" # js/ts
"vscode:dprint.dprint" = "latest" # multi-language formatter
"vscode:oxc.oxc-vscode" = "latest" # js/ts
"vscode:charliermarsh.ruff" = "latest" # python
"vscode:esbenp.prettier-vscode" = "latest" # js/html/css
"vscode:dbaeumer.vscode-eslint" = "latest" # js
"vscode:orta.vscode-jest" = "latest" # js
"vscode:vitest.explorer" = "latest" # js
"vscode:bradlc.vscode-tailwindcss" = "latest" # js/css
"vscode:mkhl.shfmt" = "latest" # bash/sh
"vscode:timonwong.shellcheck" = "latest" # bash/sh
"vscode:editorconfig.editorconfig" = "latest" # all
"vscode:johnnymorganz.stylua" = "latest"

# VS Code Themes
"vscode:catppuccin.catppuccin-vsc" = "latest"
"vscode:dracula-theme.theme-dracula" = "latest"
"vscode:enkia.tokyo-night" = "latest"
"vscode:equinusocio.vsc-material-theme" = "latest"
"vscode:equinusocio.vsc-material-theme-icons" = "latest"
"vscode:zhuangtongfa.material-theme" = "latest"
"vscode:mvllow.rose-pine" = "latest"

# VS Code Tools
"vscode:aikidosecurity.aikido" = "latest" # security
"vscode:mirone.milkdown" = "latest" # markdown editor
"vscode:ms-playwright.playwright" = "latest" # e2e testing
"vscode:aaron-bond.better-comments" = "latest"
"vscode:connor4312.esbuild-problem-matchers" = "latest"
"vscode:eamodio.gitlens" = "latest"
"vscode:foam.foam-vscode" = "latest"
"vscode:hverlin.mise-vscode" = "latest"
"vscode:ms-vscode-remote.remote-containers" = "latest"
"vscode:ms-vscode.extension-test-runner" = "latest"
"vscode:mushan.vscode-paste-image" = "latest"
"vscode:pomdtr.excalidraw-editor" = "latest"
"vscode:ryu1kn.partial-diff" = "latest"
"vscode:anthropic.claude-code" = "latest"

[bootstrap.brew.taps]
"silverstein/tap" = "https://github.com/silverstein/homebrew-tap.git"

