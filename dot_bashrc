# Load cross shell config
source ~/.config/shell/foundation.sh

# Load custom shell integrations
eval "$(mise activate bash)"
eval "$(fzf --bash)"
eval "$(zoxide init bash --cmd cd)"
eval "$(starship init bash)"
. "/Users/simon/.acme.sh/acme.sh.env"
